/**
 * @file {{ filename }}
 * @author Markus Blechschmidt (blechschmidt@simplexion.de)
 * @brief Autogenerated board configuration file
 * @version 0.1
 * @date {{ date }}
 *
 * @copyright Copyright (c) 2023 Simplexion GmbH
 *
 */

#ifndef {{ filename.upper().replace(".", "_") }}
#define {{ filename.upper().replace(".", "_") }}

#define BOARD_NAME ("{{ board }}")
#define MCU_NAME ("{{ mcu }}")
{% for connection in connections %}
#define {{ '%-15s'|format(connection.net+"_PAD") }} ({{ connection.pin }})
{%- endfor %}

#endif // {{ filename.upper().replace(".", "_") }}

